package cache

import (
	"testing"
)

func TestInit(t *testing.T) {
	// Init()
	//传入我的id和我朋友列表的id，返回对应朋友的第一条消息，若未命中则未nil，以及err
	// t.Logf("hash_key(10, 2)=%v, hash_key(2, 10)=%v, hash_key(2, 11)=%v", hash_key(10<<50, 2<<32), hash_key(2<<32, 10<<50), hash_key(2, 11))
	// fmt.Println(hash_key(10, 2), hash_key(2, 10), hash_key(2, 11))
}
